[
  {
    "name": "${name}",
    "image": "${image}",
    "cpu": ${cpu},
    "memory": ${memory},
    "essential": ${essential},
    "networkMode": "${networkMode}",
    "portMappings": [
      {
        "containerPort": ${containerPort},
        "hostPort": ${hostPort}
      }
    ]
  }
]